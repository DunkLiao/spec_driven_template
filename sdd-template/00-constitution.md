# 專案憲章 (Constitution)

> 這份文件定義整個專案**永遠**要遵守的規矩。
> 每次叫 AI 寫程式前，都讓它先讀這份。一個專案只需要一份，長期沿用。

---

## 1. 專案一句話

<!-- 用一句話講清楚這個專案是什麼。例：給風管部同仁查授信戶風險的內部小工具。 -->

[一句話描述]

## 2. 技術選型 (Tech Stack)

> 鎖定技術，避免 AI 每次用不同套件。沒用到的整段刪掉。

| 類別 | 選用 | 備註 |
|------|------|------|
| 語言 | [例：Python 3.12] | |
| 框架 | [例：FastAPI] | |
| 前端 | [例：純 HTML + Tailwind，不用前端框架] | |
| 資料庫 | [例：SQLite] | |
| 測試 | [例：pytest] | |
| 套件管理 | [例：uv] | |

## 3. 程式風格 (Coding Style)

- 命名：[例：變數 snake_case、類別 PascalCase]
- 註解：[例：公開函式一律寫 docstring，用繁體中文]
- 檔案長度：[例：單檔超過 300 行就要拆]
- 格式化工具：[例：ruff format，提交前一定跑]

## 4. 架構原則 (Principles)

- [例：商業邏輯與 I/O 分離，邏輯不直接碰資料庫]
- [例：對外輸入一律驗證，不信任使用者輸入]
- [例：能用標準函式庫就不裝第三方套件]
- [例：每個功能都要有對應測試]

## 5. 絕對禁止 (Never Do)

> AI 最容易踩雷的地方寫在這。

- ❌ [例：不要把密碼、金鑰寫死在程式碼裡]
- ❌ [例：不要為了通過測試而修改測試本身]
- ❌ [例：沒講過的套件不要自己裝]
- ❌ [例：不要一次改很多檔案，一次專注一個任務]
- ❌ [例：不確定就停下來問，不要腦補需求]

## 6. AI 協作規範

- 動工前先讀：本憲章 + 該功能的 `spec` 與 `design`。
- 每完成一個任務就停下來，讓我確認後再繼續。
- 改動超出當前任務範圍前，先說明原因並徵得同意。
- 回報時老實講：測試沒過就說沒過，跳過的步驟要講。
