# 技術設計：[功能名稱]

> 階段二：講「怎麼做」。對應的規格：`01-spec.md`。
> 動筆前先讀 `00-constitution.md`，設計不可違反憲章。

- 狀態：草稿 / 審查中 / 已定案
- 最後更新：[YYYY-MM-DD]

---

## 1. 設計概述 (Overview)

<!-- 一段話講整體做法，讓人三十秒看懂架構。 -->

[整體技術方案描述]

## 2. 架構與資料流 (Architecture)

```
[使用者] ──▶ [入口/介面層] ──▶ [商業邏輯層] ──▶ [資料層]
                                   │
                              (這裡是核心邏輯)
```

<!-- 用上面這種簡單方塊圖說明各層職責與呼叫方向。 -->

- 介面層：[負責什麼]
- 邏輯層：[負責什麼]
- 資料層：[負責什麼]

## 3. 模組與檔案規劃 (Modules)

| 檔案 / 模組 | 職責 | 對外提供 |
|-------------|------|----------|
| `[path/xxx.py]` | [做什麼] | [主要函式 / 類別] |
| `[path/yyy.py]` | [做什麼] | [主要函式 / 類別] |

## 4. 資料模型 (Data Model)

> 主要的資料結構 / 資料表 / 物件欄位。

```
[實體名稱]
  - 欄位1：型別  // 說明
  - 欄位2：型別  // 說明
```

## 5. 介面 / API 契約 (Contract)

> 函式簽章、API 路徑、輸入輸出格式。AI 之後照這個實作。

```
[例]
GET /api/borrower/{id}
  輸入：id (統編)
  輸出：{ "id": str, "risk_level": str, "updated_at": str }
  錯誤：404 查無資料 / 400 統編格式錯誤
```

## 6. 關鍵決策與取捨 (Decisions)

> 為什麼選 A 不選 B，寫下來避免之後反覆。

- **決策 1**：選用 [方案]，因為 [理由]；放棄 [替代方案]，因為 [理由]。
- **決策 2**：……

## 7. 風險與對策 (Risks)

| 風險 | 影響 | 對策 |
|------|------|------|
| [例：資料量大查詢慢] | [影響] | [加索引 / 分頁] |

## 8. 測試策略 (Testing)

- 單元測試：[要測哪些核心邏輯]
- 邊界測試：[對應 spec 的例外情境]
- 每條驗收標準 (AC) 至少對應一個測試。
