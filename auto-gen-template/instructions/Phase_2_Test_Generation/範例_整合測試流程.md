# 整合測試流程 範例

提供跨服務的整合測試腳本範例與環境設定。

```typescript
// 範例整合測試 (pseudo)
import request from 'supertest';
import app from '../../app';

test('建立訂單並完成付款', async () => {
  const res = await request(app).post('/orders').send({ items: [...] });
  expect(res.status).toBe(201);
});
```
