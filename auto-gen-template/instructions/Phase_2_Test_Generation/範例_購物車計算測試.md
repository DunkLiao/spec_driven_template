# 購物車計算測試 範例

自動生成覆蓋常見價格計算邊界的測試案例。

```typescript
import { calculateTotal } from '../Phase_3_Implementation/cart_service';

test('含折扣與稅金的總價', () => {
  const items = [{ price: 100, qty: 2 }, { price: 50, qty: 1 }];
  const total = calculateTotal(items, { discount: 0.1, tax: 0.05 });
  expect(total).toBeCloseTo(225);
});
```
