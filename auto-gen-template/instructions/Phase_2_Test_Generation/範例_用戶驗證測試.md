# 用戶驗證測試 範例

此檔示範如何為使用者驗證流程產生單元與整合測試案例。

## 測試要點
- 成功登入
- 錯誤密碼
- 帳號不存在

```typescript
import { login } from '../Phase_3_Implementation/使用者驗證';

test('成功登入', async () => {
  const res = await login('user@example.com', 'correct_password');
  expect(res.status).toBe(200);
});
```
