# 通知重試測試 範例

自動生成通知在弱連線或外部服務失敗時的重試與回退測試。

```typescript
import { sendNotification } from '../Phase_3_Implementation/notification_sender';

test('失敗後重試並成功', async () => {
  const res = await sendNotification('user-id', { message: 'Hi' });
  expect(res.success).toBe(true);
});
```
