# 實作：支付處理 範例

簡單示範與第三方金流互動的封裝與錯誤處理。

```typescript
export async function charge(cardInfo, amount) {
  // 呼叫第三方金流 SDK (pseudo)
  const resp = await paymentGateway.charge({card: cardInfo, amount});
  if (!resp.success) throw new Error('Payment failed');
  return resp;
}
```
